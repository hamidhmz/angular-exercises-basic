import { EgRendererDirective } from './eg-renderer.directive';

describe('EgRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new EgRendererDirective();
    expect(directive).toBeTruthy();
  });
});
